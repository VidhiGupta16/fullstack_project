import { PageFrame } from '../../components/common/PageFrame'

export function ActionItemListPage() {
  return <PageFrame eyebrow="Action Items" title="Action Item List" description="Create, edit, delete, assign owner, priority, status, and due dates are supported in the design." />
}
