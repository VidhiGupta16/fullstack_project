import { PageFrame } from '../../components/common/PageFrame'

export function AuditLogViewerPage() {
  return <PageFrame eyebrow="Audit Logs" title="Audit Log Viewer" description="Who, what, when, and affected resource are tracked here." />
}
