import { PageFrame } from '../../components/common/PageFrame'

export function RecordingUploadPage() {
  return <PageFrame eyebrow="Recording" title="Upload Recording" description="Drag and drop, progress bar, retry upload, and status tracking are ready." />
}
