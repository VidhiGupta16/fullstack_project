import { PageFrame } from '../../components/common/PageFrame'

export function ResetPasswordPage() {
  return <PageFrame eyebrow="Authentication" title="Reset Password" description="Reset password page scaffold is ready." />
}
